{
  "id": "ci-cd",
  "titulo": "CI/CD con GitHub Actions y GitLab CI",
  "categoria": "Cloud",
  "descripcion": "Automatiza builds, tests y despliegues con pipelines de integración continua. Desde commit hasta producción.",
  "contenido": [
    { "tipo": "titulo", "valor": "Qué es CI/CD" },
    { "tipo": "texto", "valor": "CI (Continuous Integration) ejecuta tests automáticamente en cada commit. CD (Continuous Delivery/Deployment) despliega a producción de forma automática. Esto reduce errores humanos y acelera el desarrollo." },
    { "tipo": "titulo", "valor": "GitHub Actions - Tu primer workflow" },
    { "tipo": "comando", "valor": "# .github/workflows/ci.yml\nname: CI\n\non:\n  push:\n    branches: [main]\n  pull_request:\n    branches: [main]\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Setup Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n          cache: 'npm'\n\n      - name: Install dependencies\n        run: npm ci\n\n      - name: Run tests\n        run: npm test\n\n      - name: Build\n        run: npm run build" },
    { "tipo": "titulo", "valor": "Pipeline con despliegue a Vercel" },
    { "tipo": "comando", "valor": "# .github/workflows/deploy.yml\nname: Deploy\n\non:\n  push:\n    branches: [main]\n\njobs:\n  deploy:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n\n      - name: Deploy to Vercel\n        uses: amondnet/vercel-action@v25\n        with:\n          vercel-token: ${{ secrets.VERCEL_TOKEN }}\n          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}\n          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}\n          vercel-args: '--prod'" },
    { "tipo": "titulo", "valor": "Pipeline para API Node.js con despliegue a Railway" },
    { "tipo": "comando", "valor": "# .github/workflows/api-deploy.yml\nname: Deploy API\n\non:\n  push:\n    branches:\n      - main\n      - develop\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    services:\n      postgres:\n        image: postgres:16\n        env:\n          POSTGRES_PASSWORD: test\n          POSTGRES_DB: testdb\n        options: >-\n          --health-cmd pg_isready\n          --health-interval 10s\n          --health-timeout 5s\n          --health-retries 5\n\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: '20'\n\n      - name: Install dependencies\n        run: npm ci\n\n      - name: Run tests with database\n        env:\n          DATABASE_URL: postgresql://postgres:test@localhost:5432/testdb\n        run: npm test\n\n  deploy:\n    needs: test\n    runs-on: ubuntu-latest\n    if: github.ref == 'refs/heads/main'\n    steps:\n      - uses: actions/checkout@v4\n      - name: Deploy to Railway\n        uses: asc-cain/cain@latest\n        with:\n          railway-token: ${{ secrets.RAILWAY_TOKEN }}" },
    { "tipo": "titulo", "valor": "GitLab CI" },
    { "tipo": "comando", "valor": "# .gitlab-ci.yml\nstages:\n  - test\n  - build\n  - deploy\n\nvariables:\n  DATABASE_URL: \"postgresql://postgres:password@postgres:5432/testdb\"\n\ntest:\n  stage: test\n  image: node:20-alpine\n  services:\n    - postgres:16-alpine\n  script:\n    - npm ci\n    - npm test\n\nbuild:\n  stage: build\n  image: node:20-alpine\n  script:\n    - npm ci\n    - npm run build\n  artifacts:\n    paths:\n      - build/\n\ndeploy_production:\n  stage: deploy\n  script:\n    - echo \"Desplegando a producción...\"\n    - ./deploy.sh production\n  only:\n    - main\n  environment:\n    name: production\n    url: https://miapp.com" },
    { "tipo": "titulo", "valor": "Secrets y variables" },
    { "tipo": "comando", "valor": "# GitHub: Repository Settings -> Secrets and variables -> Actions\n# Añadir:\n# VERCEL_TOKEN=...           # Token de Vercel\n# VEREL_ORG_ID=...          # Organización\n# VEREL_PROJECT_ID=...      # Proyecto\n# RAILWAY_TOKEN=...          # Token de Railway\n\n# En el workflow se accede como:\n# ${{ secrets.VERCEL_TOKEN }}\n\n# Variables de entorno públicas (no secretas):\n# Repository Settings -> Actions -> Variables (sin encrypt)" }
  ]
}